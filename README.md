A project to export final prices from Booli.

The notebook called `slutpriser.ipynb` scrapes the content from an URL and dump it to a csv.

The idea is to create different plotting scripts too see trends.